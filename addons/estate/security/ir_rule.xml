<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <record id="rule_estate_property_restricted" model="ir.rule">

        <field name="name">Properties - Hide Restricted</field>

        <field name="model_id" ref="model_estate_property"/>

        <field name="groups" eval="[(4, ref('estate.group_estate_restricted'))]"/>

        <field name="domain_force">[('restricted', '=', False)]</field>

    </record>

</odoo>